import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Minimal configuration - appIsrStatus is not a valid property in Next.js 16.1.6
  // If you need dev indicators, use: devIndicators: { position: 'bottom-right' }
};

export default nextConfig;
